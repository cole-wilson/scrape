def main():
  import os
  os.system('git config user.name --global "repl.it bot";git config user.email --global "t";git init;git add *;git commit * -m "commit"')
  os.system('git push https://github.com/cole-wilson/s')